/** Re-export from bridge schema so the frontend and bridge share one definition. */
export type {
  DrumPad,
  Step,
  StepGrid,
  LockState,
  ActiveMode,
  SequencerState,
} from "../../bridge/schema";
